// backend/src/http/routeDeps.js
// Mutable bag of dependencies for domain route modules (populated from server.js).

/** @type {Record<string, unknown>} */
export const routeDeps = Object.create(null);
